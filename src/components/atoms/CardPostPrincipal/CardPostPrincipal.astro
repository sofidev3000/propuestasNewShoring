---
const {animationDirection='fade-up'} = Astro.props;
const { link, title,image,altdesc, autor,date,slot} = Astro.props;

---

<a slot={slot} href={link} class="flex flex-col self-center w-full max-w-[600px] 2xl:max-w-[708px] group">
<article  data-aos={animationDirection} class="">
    <div class="w-full lg:max-w-[600px] 2xl:max-w-[708px] overflow-hidden ">
      <img
        class="w-full h-full object-cover object-center group-hover:scale-125 ease-in-out duration-500 transition-transform"
        src={image}
        alt={altdesc}
      />
    </div>
    <h3 class="text-[1.3rem] ">
        {title}
    </h3>
    <div class="flex">
      <p>
        Por <strong>{autor}</strong> - <span>{date} </span>
      </p>
    </div>
  </article>
</a>